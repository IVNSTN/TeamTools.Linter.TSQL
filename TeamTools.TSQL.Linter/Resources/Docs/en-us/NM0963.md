

# Table naming convention violation - lower_snake_case expected

|||
|-|-|
| Id | **NM0963**
| Mnemo | TABLE_NAME_LOWER_SNAKE_CASE
| Severity | ℹ Hint
| Category | [Naming](./Category_Naming.md)
| Source code | [TableViewNameLowerSnakeCaseRule.cs](../../../Rules/Naming/TableViewNameLowerSnakeCaseRule.cs)

## Cause

The rule is triggered if the project settings specify
that the table name must match the lower_snake_case notation,
but it does not match.

<p id="descr">Use lower_snake_case notation.</p>

## Examples

Bad

```sql
CREATE TABLE dbo.regCustomers (id BIGINT, name NVARCHAR(50));
```

Good

```sql
CREATE TABLE dbo.reg_customers (id BIGINT, name NVARCHAR(50));
```

## Tips

💡 The rule also applies to temporary tables # and table variables @

💡 Requires such notation only in projects where it is explicitly specified in the settings.

## Links

The rule is mutually exclisive with rules [NM0964](./NM0964.md),  [NM0965](./NM0965.md)
