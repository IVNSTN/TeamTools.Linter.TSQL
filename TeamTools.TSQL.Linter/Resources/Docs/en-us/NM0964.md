

# Table naming convention violation - UPPER_SNAKE_CASE expected

|||
|-|-|
| Id | **NM0964**
| Mnemo | TABLE_NAME_UPPER_SNAKE_CASE
| Severity | ℹ Hint
| Category | [Naming](./Category_Naming.md)
| Source code | [TableViewNameUpperSnakeCaseRule.cs](../../../Rules/Naming/TableViewNameUpperSnakeCaseRule.cs)

## Cause
The rule is triggered if the project settings specify
that the table name must match the UPPER_SNAKE_CASE notation,
but it does not match.

<p id="descr">Use UPPER_SNAKE_CASE notation.</p>

## Examples

Bad

```sql
CREATE TABLE dbo.regCustomers (id BIGINT, name NVARCHAR(50));
```

Good

```sql
CREATE TABLE dbo.REG_CUSTOMERS (id BIGINT, name NVARCHAR(50));
```

## Tips

💡 The rule also applies to temporary tables # and table variables @

💡 Requires such notation only in projects where it is explicitly specified in the settings.

## Links

The rule is mutually exclisive with rules [NM0963](./NM0963.md),  [NM0965](./NM0965.md)
