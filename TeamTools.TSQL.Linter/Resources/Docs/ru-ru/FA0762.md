

# Свойство не поддерживается для in-memory таблиц

|||
|-|-|
| Id | **FA0762**
| Мнемо | INMEMORY_RESTRICTED_TABLE_OPTIONS
| Серьёзность | ⛔ Ошибка
| Категория | [Сбой](./Категория_Сбой.md), [ИнМемори](./Группа_ИнМемори.md)
| Исходный код | [InMemoryRestrictedTableOptionsRule.cs](../../../Rules/Failure/InMemoryRestrictedTableOptionsRule.cs)

## Причина

Правило срабатывает, если свойство не поддерживается для in-memory таблиц.
<p id="descr">Удалите неподдерживаемое свойство.</p>

## Примеры

Некорректно

```sql
CREATE TABLE foo.bar
(
    sys_name        VARCHAR(20)  NOT NULL
    , sign_datetime DATETIME2(0) NOT NULL
    , CONSTRAINT PK_foo_bar PRIMARY KEY NONCLUSTERED HASH (sys_name) WITH (BUCKET_COUNT = 8000000)
)
WITH (MEMORY_OPTIMIZED = ON, DURABILITY = SCHEMA_AND_DATA, DATA_COMPRESSION = PAGE);
```

Корректно

```sql
CREATE TABLE foo.bar
(
    sys_name        VARCHAR(20)  NOT NULL
    , sign_datetime DATETIME2(0) NOT NULL
    , CONSTRAINT PK_foo_bar PRIMARY KEY NONCLUSTERED HASH (sys_name) WITH (BUCKET_COUNT = 8000000)
)
WITH (MEMORY_OPTIMIZED = ON, DURABILITY = SCHEMA_AND_DATA);
```

## Ссылки

💡 [Неподдерживаемые возможности SQL Server в In-Memory OLTP](https://learn.microsoft.com/ru-ru/sql/relational-databases/in-memory-oltp/unsupported-sql-server-features-for-in-memory-oltp?view=sql-server-ver16)
