

# Нарушение соглашения об именовании таблиц - ожидается UPPER_SNAKE_CASE

|||
|-|-|
| Id | **NM0964**
| Мнемо | TABLE_NAME_UPPER_SNAKE_CASE
| Серьёзность | ℹ Подсказка
| Категория | [Именование](./Категория_Именование.md)
| Исходный код | [TableViewNameUpperSnakeCaseRule.cs](../../../Rules/Naming/TableViewNameUpperSnakeCaseRule.cs)

## Причина
Правило срабатывает, если в настройках проекта указано, 
что наименование таблиц должно соответствовать нотации UPPER_SNAKE_CASE,
но оно не соответствует.

<p id="descr">Используйте нотацию UPPER_SNAKE_CASE.</p>

## Примеры

Некорректно

```sql
CREATE TABLE dbo.regCustomers (id BIGINT, name NVARCHAR(50));
```

Корректно

```sql
CREATE TABLE dbo.REG_CUSTOMERS (id BIGINT, name NVARCHAR(50));
```

## Подсказки

💡 Правило также распространяется на временные таблицы # и табличные переменные @

💡 Требует такую нотацию только в проектах, где это явно указано в настройках.

## Ссылки

Правило несовместимо с правилами [NM0963](./NM0963.md), [NM0965](./NM0965.md)
