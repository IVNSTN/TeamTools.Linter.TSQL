

# Нарушение соглашения об именовании таблиц - ожидается PascalCase

|||
|-|-|
| Id | **NM0965**
| Мнемо | TABLE_NAME_PASCAL_CASE
| Серьёзность | ℹ Подсказка
| Категория | [Именование](./Категория_Именование.md)
| Исходный код | [TableViewNamePascalCaseRule.cs](../../../Rules/Naming/TableViewNamePascalCaseRule.cs)

## Причина
Правило срабатывает, если в настройках проекта указано, 
что наименование таблиц должно соответствовать нотации PascalCase,
но оно не соответствует.

<p id="descr">Используйте нотацию PascalCase.</p>

## Примеры

Некорректно

```sql
CREATE TABLE dbo.reg_customers (id BIGINT, name NVARCHAR(50));
```

Корректно

```sql
CREATE TABLE dbo.RegCustomers (id BIGINT, name NVARCHAR(50));
```

## Подсказки

💡 Правило также распространяется на временные таблицы # и табличные переменные @

💡 Требует такую нотацию только в проектах, где это явно указано в настройках.

## Ссылки

Правило несовместимо с правилами [NM0963](./NM0963.md), [NM0964](./NM0964.md)
