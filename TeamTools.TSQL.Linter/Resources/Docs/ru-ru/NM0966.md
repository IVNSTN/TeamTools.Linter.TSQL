

# Нарушение соглашения об именовании полей - ожидается lower_snake_case

|||
|-|-|
| Id | **NM0966**
| Мнемо | FIELD_NAME_LOWER_SNAKE_CASE
| Серьёзность | ℹ Подсказка
| Категория | [Именование](./Категория_Именование.md)
| Исходный код | [FieldNameLowerSnakeCaseRule.cs](../../../Rules/Naming/FieldNameLowerSnakeCaseRule.cs)

## Причина
Правило срабатывает, если в настройках проекта указано, 
что наименование колонок должно соответствовать нотации lower_snake_case,
но оно не соответствует.

<p id="descr">Используйте нотацию lower_snake_case.</p>

## Примеры

Некорректно

```sql
CREATE TABLE dbo.reg_customers (id BIGINT, name NVARCHAR(50), cityId INT);
```

Корректно

```sql
CREATE TABLE dbo.reg_customers (id BIGINT, name NVARCHAR(50), city_id INT);
```

## Подсказки

💡 Правило также распространяется на вью, табличные функции, процедуры, временные таблицы # и табличные переменные @

💡 Требует такую нотацию только в проектах, где это явно указано в настройках.

## Ссылки

Правило несовместимо с правилами [NM0967](./NM0967.md), [NM0968](./NM0968.md)
