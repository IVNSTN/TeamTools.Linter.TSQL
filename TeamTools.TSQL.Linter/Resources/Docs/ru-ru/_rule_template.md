
# {Сообщение о нарушении правила}

|||
|:-|:-|
| Id | **{Id правила}**
| Мнемо | {Мнемо-код правила}
| Серьёзность | {⛔, ⚠ или ℹ} {Уровень серьёзности нарушения: Ошибка, Предупреждение или Подсказка}
| Категория | [{Название группы правил}](./Группа_{Имя файла группы правил}.md)[,.. ссылки на дополнительные группы и категории]
| Исходный код | [{Имя файла с исходным кодом правила}.cs](../../../Rules/{Поддиректория}/{Имя файла с исходным кодом правила}.cs)

## Причина

{Описание причины срабатывания правила, например: Правило срабатывает, если обнаружено обращение к объектам других баз без использования синонимов.}

<p id="descr">{Краткая рекомендация, что нужно сделать, что бы исправить обнаруженную ошибку. Этот текст будет отображаться во всплывающей подсказке в интерфейсе VS. Обратите внимание, этот элемент не может содержать вложенных элементов HTML- или маркдаун-разметки. Пример: Для обращения к объектам других баз используйте синонимы.}</p>

## Примеры

Некорректно

```sql
{Пример кода T-SQL с нарушением.
Пример (или несколько) должны быть минималистично необходимыми, чтобы показать ошибку. Развесистых конструкций не нужно. Разделять, при помощи GO не нужно, это просто примеры. Если с составлением примеров совсем плохо, можно подсмотреть тут TSQLLinter\TeamTools.TSQL.LinterTests\UnitTests}
```

Корректно

```sql
{Пример исправленного кода T-SQL}
```

## Подсказки

⚠️ {Одно или несколько важных замечаний, на которые нужно обратить внимание.}

💡 {Один или несколько полезных комментариев.}

## Ссылки

{Здесь можно разместить ссылки на статьи документации MS SQL Server или на файлы документации связанных по смыслу правил линтера.}
