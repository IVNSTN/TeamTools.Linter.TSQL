-- scriptdom parses MAX as Lileral which is descandant from PrimaryExpression same as FunctionCall
DECLARE @str VARCHAR(MAX)

DECLARE @tbl TABLE
(
    title VARCHAR(MAX)
)

CREATE TABLE dbo.tbl
(
    title VARCHAR(MAX)
)
