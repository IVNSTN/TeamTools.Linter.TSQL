SELECT bar, far
FROM zar
INNER JOIN aaa ON aa = zz
LEFT JOIN bbb ON bb = aa
CROSS JOIN ccc
FULL JOIN ddd ON dd = cc
OUTER APPLY (SELECT 1 as ee) AS ouappl
CROSS APPLY (SELECT 2 as ff) AS crappl
JOIN iii ON ii = ff
WHERE dar <> gar
ORDER BY mar, nar, jar
